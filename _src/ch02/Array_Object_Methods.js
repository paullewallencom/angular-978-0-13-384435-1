METHOD                      DESCRIPTION
concat(arr1, arr2, ...)     Returns a joined copy of the array and the 
                            arrays passed as arguments.
indexOf(value)              Returns the first index of the value in the array
                            or -1 if the item is not found.
join(separator)             Joins all elements of an array, separated by the 
                            separator into a single string. If no separator is 
                            specified, a comma is used.
lastIndexOf(value)          Returns the last index of the value in the array or 
                            -1 if the value is not found.
pop()                       Removes the last element from the array and returns 
                            that element.
push(item1, item2, ...)     Adds one or more new elements to the end of an 
                            array and returns the new length.
reverse()                   Reverses the order of all elements in the array.
shift()                     Removes the first element of an array and returns 
                            that element.
slice(start, end)           Returns the elements between the start and 
                            end indexes.
sort(sortFunction)          Sorts the elements of the array. 
                            sortFunction is optional.
splice(index, count, item1, At the index specified, removes count number items 
  item2, ...)               and then inserts at index any optional items passed 
                            in as arguments.
toString()                  Returns the string form of an array.
unshift()                   Adds new elements to the beginning of an array and 
                            returns the new length.
valueOf()                   Returns the primitive value of an Array object.